# Nivora scripts

`android-setup.ps1` is a Windows PowerShell helper for preparing the Capacitor Android project locally.

Run it from the repository root after cloning:

```powershell
.\scripts\android-setup.ps1
```

The script installs dependencies, builds the web app, creates the Android project when needed, syncs assets, and opens Android Studio.
